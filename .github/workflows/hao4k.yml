name: hao4k

on:
  push:
    branches: 
      - main
  schedule:
    - cron: '0 8,22 * * *'

jobs:
  Hao4K-autoSignIn:
    runs-on: ubuntu-latest
    
    steps:
    - name: checkout
      uses: actions/checkout@master
    
    - name: Set python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'
    
    - name: Install dependencies
      run: python -m pip install --upgrade requests
    
    - name: Hao4k auto sign in
      env:
        HAO4K_USERNAME: ${{ secrets.wol8782 }}
        HAO4K_PASSWORD: ${{ secrets.Xinxin201394 }}
        SERVERCHAN_SCKEY: ${{ secrets.SCT165217TL7UL4Kvv5EKpXlxnKK6WD99y }}
      run: python signin.py
